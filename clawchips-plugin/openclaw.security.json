{
  "$schema": "https://openclaw.ai/schemas/plugin-security.json",
  "version": "1.0",
  "plugin": "clawchips",
  "permissions": {
    "network": {
      "outbound": ["*"],
      "reason": "Embedding API (OpenAI-compatible /v1/embeddings) and optional provider endpoints"
    },
    "filesystem": {
      "read": ["**/*.yaml", "**/*.yml", "**/*.json", "~/.openclaw/**"],
      "write": ["~/.openclaw/**", "~/.clawchips/**"],
      "reason": "Read/write routing YAML, SQLite (memory + dashboard storage)"
    }
  },
  "dataFlow": {
    "promptsLeaveProcess": true,
    "description": "User prompts are embedded for routing memory search; may be sent to configured embedding endpoint only."
  },
  "securityNotes": [
    "Configure embedding endpoint and API keys only in trusted environments",
    "Dashboard HTTP routes require plugin auth when exposed by OpenClaw"
  ]
}
